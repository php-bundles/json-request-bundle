filter:
    excluded_paths:
        - tests/*
        - vendor/*

checks:
    php:
        code_rating: true
        duplication: true

build:
    tests:
        override:
            -
                command: vendor/bin/phpunit
